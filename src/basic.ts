export function addition(a: number, b: number, c: number): number {
    //  Add three numbers together, return result
    return a + b + c;
}

export function addition2(a: number, b: number): number {
    //  Add three numbers together, return result
    return a + b;
}
